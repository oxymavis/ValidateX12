# EDI 210 增强校验逻辑说明

本文档对应 `edi_210_validator.py` 的当前实现。

默认模式按 BluJay 210 spec 的显式 `Mandatory/Required` 规则执行；文档中带 `recommended`、`may be required`、`per shipper invoice validation profile` 的项目默认不作为硬错误。若需要旧的企业强校验口径，可传 `strict_profile=True`。

## 1) 包络层

### ISA / IEA

- 必须存在：`ISA`、`IEA`
- `ISA01=00`、`ISA03=00`
- `ISA05` in `{02, ZZ}`
- `ISA07` in `{01, ZZ}`
- `ISA11=U`
- `ISA12=00401`
- `ISA14` in `{0,1}`
- `ISA15=P`
- `ISA16` 推荐 `>`（Warning）
- `IEA01 = GS 数量`
- `IEA02 = ISA13`

相关错误码：`E001 E002 E003 E004`

### GS / GE

- `GS01=IM`
- `GS04` 日期有效（`CCYYMMDD`）
- `GS05` 时间有效（`HHMM` 或 `HHMMSS`）
- `GS07=X`
- `GS08=004010`
- `GE01 = ST 数量`
- `GE02 = GS06`

相关错误码：`E010 E011 E012`

### ST / SE

- `ST01=210`
- `ST02` 必填
- `SE02 = ST02`
- `SE01 = ST..SE 实际段数`

相关错误码：`E020 E021 E022`

## 2) 主体结构层

- 关键段存在性：`B3`、`C3`、`LX`、`L3`
- 基本顺序：`B3` 应在首个 `LX` 前

## 3) 字段层

### B3

- 必填：`B302/B303/B304/B306/B307/B311`
- `B304` in `{CC, PP}`
- `B308` 若有值，in `{BD, CO}`
- `B306` 有效日期
- `B307` 数值
- `B311` 长度 2-4
- `B303` 限制为字母数字、下划线、连字符（禁止空格和特殊字符）

相关错误码：`E100 E101 E102 E103 E104 E105 E106 E107`

### C3

- 必须存在
- `C301` 必填
- `C301` 限制 `USD/CAD/MXN/EUR`

相关错误码：`E110 E111 E112`

### N9

- `N901` 必填
- `N902` 或 `N903` 至少一个有值
- 缺少 `MB` / `PO` 以 Warning 给出

相关错误码：`E120 E121 E122 E123`

### Heading N1 Loop

- Heading 区至少一个 `N1`
- 必须存在 `N1*SH`
- `N102` 或 `N103+N104`
- `N103/N104` 成对
- `N4` 若有国家码，校验 `N404`（2-3 位字母）

相关错误码：`E130 E131 E132 E134`

### S5 / G62 / 0310 Location

- `S501` 必填、不可重复、建议递增
- `S502` 必须 `LD` 或 `UL`
- 必须同时出现至少一个 `LD` 和一个 `UL`
- `G62` 日期对/时间对必须成对
- `G6201` in `{35,86}`
- `G6203` in `{8,9}`
- `G6205` 若有，in `{CD,CT,ED,ET,MD,MT,PD,PT}`
- `S5` 整体、`G62`、0310 `N1` 属于推荐/条件性规则，默认给 Warning 或仅在出现时校验
- 0310 `N1` 若出现，则 `N101` 必须 `SF` 或 `ST`
- 0310 `N3` 仅在对应的 `N1` loop 出现时校验
- 若发送 G62 actual status，pickup stop 推荐 `SF`，delivery stop 推荐 `ST`

相关错误码：`E200 E201 E202 E203 E210 E211 E212 E213 E214 E220 E221 E222 E223 E224`

### LX / L0 / L1 / L7 / K1

- `LX01` 必填、正整数、递增
- 每个 LX：`L0` 最多 1 个，`L1` 必须且最多 1 个

L0:
- `L002/L003` 成对
- `L004/L005` 成对
- `L008/L009` 成对
- `L003` in `{DM,LB}`
- `L005` in `{B,F,G,N,T}`
- 当 `L1-03` in `{PM,CW}` 时，`L0` 必须存在

L1:
- `L102/L103` 成对
- `L104/L105/L106` 至少一个
- `L103` in `{CW,FR,PM,PP,PT,PU,RP,ST}`
- `L108` 格式 `2-4` 位大写字母/数字
- `L108=999` 时 `L112` 必填
- 金额字段数值校验：`L102/L104/L105/L106`

L7:
- 若传，校验 `L707` 格式

K1:
- `K101` 必填，长度 <= 30

相关错误码：`E300 E301 E310 E311 E312 E313 E314 E315 E320 E321 E322 E323 E324 E325 E330 E340`

### SPO / 0460

SPO:
- `SPO01` 必填
- `SPO03/SPO04` 成对
- `SPO05/SPO06` 成对

0460 N1/N3:
- `N101` 必须 `SF/ST`
- 若有 0460 `N1`，必须有 `N3`

相关错误码：`E350 E351 E352 E360 E361`

## 4) Summary / 业务层

### L3

- `L301/L302` 成对
- `L303/L304` 成对
- `L309/L310` 成对
- `L302` in `{B,F,G,N,T}`
- `L304` 使用与 L1 相同的常用 rate qualifier 集合
- `L308` 格式校验
- `L310` in `{E,X}`
- `L305` 数值，且等于 `sum(L1-04)`

相关错误码：`E400 E401 E402 E403 E404 E405 E406 E407`

### 业务交叉

- 若提供 `expected_204`：
  - `B3-03` 对比 `expected_204['B204']`（`E500`）
  - `B3-11` 对比 `expected_204['B202']`（`E501`）
- 至少 1 条 charge line (`L1`)：`E530`

## 5) 后续可继续增强（未内置）

- `N4` 国家码按完整 ISO-3166 白名单校验
- `L108/L308` 按完整 Appendix A code list 校验
- `L304` 按完整 Appendix B code list 校验
- 多 ST/SE、多 GS/GE 的严格分组配对校验
